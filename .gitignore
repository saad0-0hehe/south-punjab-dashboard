# Environments
.env
.venv
env/
venv/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# IDEs
.vscode/
.idea/

# OS generated
.DS_Store
Thumbs.db

# Temporary and local outputs (if any)
# We will keep the outputs/ folder if it has generated charts that the user wants to commit, but generally generated files shouldn't be committed. However, it's a portfolio project, they might want them there.
